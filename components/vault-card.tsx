"use client";
import { motion } from "framer-motion";
import { Vault } from "@/lib/config";
import { StatusBadge } from "./shell";

export function VaultCard({ vault, connecting, onConnect }: { vault: Vault; connecting: boolean; onConnect: () => void }) { const isSource = vault.id === "base"; return <motion.div layout className="glass rounded-2xl p-5 transition hover:border-slate-500/70"><div className="flex items-start justify-between gap-4"><div className="flex items-center gap-3"><div className={`flex h-11 w-11 items-center justify-center rounded-xl text-lg ${isSource ? "bg-violet-400/15 text-violet-200" : "bg-cyan/10 text-cyan"}`}>{isSource ? "⌂" : "◆"}</div><div><div className="font-semibold text-white">{vault.name}</div><div className="mt-1 text-xs text-slate-500">{vault.network} · {vault.role}</div></div></div><StatusBadge status={vault.status} /></div><div className="mt-5 grid grid-cols-2 gap-3 border-t border-line pt-4"><div><div className="eyebrow">Address</div><div className="mono mt-1 text-xs text-slate-300">{vault.address}</div></div><div className="text-right"><div className="eyebrow">Balance</div><div className="mt-1 text-sm font-semibold text-white">{vault.balance.toLocaleString()} <span className="text-slate-500">{vault.symbol}</span></div></div></div>{!isSource && <button onClick={onConnect} disabled={connecting || vault.status === "Connected"} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl border border-white/10 bg-white/5 py-2.5 text-sm font-semibold text-white transition hover:bg-white/10 disabled:cursor-not-allowed disabled:opacity-50">{connecting && <span className="h-4 w-4 animate-spin rounded-full border-2 border-slate-500 border-t-cyan" />}{vault.status === "Connected" ? "Connected" : connecting ? "Connecting…" : "Connect vault"}</button>}</motion.div>; }
