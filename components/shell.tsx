"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";

export function Shell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <div className="min-h-screen grid-bg"><header className="sticky top-0 z-20 border-b border-line/80 bg-ink/85 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link href="/transfer" className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-cyan text-ink font-black">T</div><div><div className="font-semibold tracking-tight">TransferApp</div><div className="eyebrow mt-0.5">Vault operations</div></div></Link><nav className="flex items-center gap-1"><Link href="/transfer" className={`rounded-lg px-4 py-2 text-sm transition ${path === "/transfer" ? "bg-white/10 text-white" : "text-slate-400 hover:text-white"}`}>Transfer</Link></nav><span className="rounded-full border border-cyan/30 bg-cyan/10 px-3 py-1.5 text-[10px] font-bold tracking-[.18em] text-cyan">LIVE</span></div></header><main className="mx-auto max-w-7xl px-5 py-8">{children}</main><div className="fixed bottom-4 left-1/2 z-10 -translate-x-1/2 rounded-full border border-amber-300/20 bg-amber-300/10 px-4 py-2 text-xs text-amber-200">Live — no real funds</div></div>; }
export function StatusBadge({ status }: { status: string }) { const color = status === "Connected" || status === "Completed" ? "text-mint bg-mint/10 border-mint/20" : status === "Failed" ? "text-red-300 bg-red-400/10 border-red-400/20" : status === "In progress" ? "text-cyan bg-cyan/10 border-cyan/20" : "text-slate-400 bg-white/5 border-white/10"; return <span className={`inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1 text-xs ${color}`}><span className="h-1.5 w-1.5 rounded-full bg-current" />{status}</span>; }
export function SectionTitle({ eyebrow, title, description }: { eyebrow: string; title: string; description?: string }) { return <div className="mb-7"><div className="eyebrow mb-2">{eyebrow}</div><h1 className="text-3xl font-semibold tracking-tight text-white md:text-4xl">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">{description}</p>}</div>; }
