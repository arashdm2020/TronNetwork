"use client";
import { motion } from "framer-motion";
import { TRANSFER_STEPS } from "@/lib/config";
export function StepProgress({ active, failed }: { active: number; failed?: boolean }) { return <div className="space-y-2">{TRANSFER_STEPS.map((step, i) => { const done = i < active || (active === 6 && i === 6); const current = i === active; return <div key={step.id} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 transition ${current ? "bg-cyan/10" : ""}`}><div className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-full border text-xs font-bold ${failed && current ? "border-red-400 bg-red-400/15 text-red-300" : done ? "border-mint bg-mint/15 text-mint" : current ? "border-cyan bg-cyan/15 text-cyan" : "border-slate-700 text-slate-600"}`}>{done ? "✓" : i + 1}</div><div className="min-w-0 flex-1"><div className={`text-sm ${current ? "font-semibold text-white" : done ? "text-slate-300" : "text-slate-500"}`}>{step.label}</div>{current && <div className="mt-0.5 text-xs text-cyan">{failed ? "Simulation stopped here" : "Processing now"}</div>}</div>{current && !failed && <span className="h-1.5 w-1.5 animate-pulse rounded-full bg-cyan" />}</div>; })}</div>; }
