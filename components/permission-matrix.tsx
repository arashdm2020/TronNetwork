"use client";
import { useLiveStore } from "@/store/transfer-store";
const actions = ["View", "Initiate Transfer", "Approve Transfer", "Manage Vaults", "Change Permissions"];
export function PermissionMatrix() { const permissions = useLiveStore(s => s.permissions); const toggle = useLiveStore(s => s.togglePermission); return <div className="glass overflow-hidden rounded-2xl"><div className="flex items-center justify-between border-b border-line px-5 py-4"><div><div className="font-semibold text-white">Permission matrix</div><div className="mt-1 text-xs text-slate-500">Role-based controls for this presentation</div></div><span className="rounded-full border border-amber-300/20 bg-amber-300/10 px-2.5 py-1 text-[10px] font-semibold text-amber-200">Live permissions — not enforced</span></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead><tr className="border-b border-line text-xs text-slate-500"><th className="px-5 py-3 font-medium">Role</th>{actions.map(a => <th key={a} className="px-3 py-3 text-center font-medium">{a}</th>)}</tr></thead><tbody>{Object.entries(permissions).map(([role, values]) => <tr key={role} className="border-b border-line/70 last:border-0"><td className="px-5 py-4 font-medium text-white">{role}</td>{actions.map(a => <td key={a} className="px-3 py-4 text-center"><button aria-label={`${role} ${a}`} onClick={() => toggle(role, a)} className={`relative h-5 w-9 rounded-full transition ${values[a] ? "bg-cyan" : "bg-slate-700"}`}><span className={`absolute top-1 h-3 w-3 rounded-full bg-white transition ${values[a] ? "left-5" : "left-1"}`} /></button></td>)}</tr>)}</tbody></table></div></div>; }
